{
  "name": "transit-blockchain",
  "version": "0.0.1",
  "private": true,
  "description": "Blockchain local (Hardhat + Solidity) para billeteras y pagos de transporte",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
